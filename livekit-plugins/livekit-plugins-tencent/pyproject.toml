[project]
name = "livekit-plugins-tencent"
dynamic = ["version"]
description = "LiveKit Agent Plugins for Tencent"
readme = "README.md"
authors = [
    { name = "wangmengdi", email = "790990241@qq.com" },
    { name = "maxinewell"}
]
keywords = ["webrtc", "realtime", "audio", "livekit", "tencent", "stt", "tts"]
requires-python = ">=3.10"
dependencies = [
    "livekit-agents>=1.2.9",
    "aiohttp>=3.9.0",
    "pydantic>=2.0",
    "requests>=2.25.0",
]

classifiers = [
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Topic :: Multimedia :: Sound/Audio",
    "Topic :: Multimedia :: Video",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3 :: Only",
]



[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.version]
path = "livekit/plugins/tencent/version.py"

[tool.hatch.build.targets.wheel]
packages = ["livekit"]

[tool.hatch.build.targets.sdist]
include = ["/livekit"]
